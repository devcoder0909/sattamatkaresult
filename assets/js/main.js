document.addEventListener("DOMContentLoaded", () => { const root = document.documentElement; const btn = document.querySelector(".theme-toggle"); if (localStorage.getItem("theme") === "light") root.setAttribute("data-theme", "light"); if (btn) { btn.addEventListener("click", () => { const isLight = root.getAttribute("data-theme") === "light"; root.setAttribute("data-theme", isLight ? "dark" : "light"); localStorage.setItem("theme", isLight ? "dark" : "light"); }); } const menuBtn = document.querySelector(".mobile-menu-btn"); const nav = document.querySelector(".nav-links"); if (menuBtn && nav) { menuBtn.addEventListener("click", () => nav.classList.toggle("active")); } const btt = document.getElementById("back-to-top"); if (btt) { window.addEventListener("scroll", () => btt.classList.toggle("show", window.scrollY > 300)); btt.addEventListener("click", () => window.scrollTo({top: 0, behavior: "smooth"})); } const searchInput = document.getElementById("searchInput"); const searchResults = document.getElementById("searchResults"); let searchData = []; if (searchInput) { fetch(ROOT_PATH + "search-index.json").then(res => res.json()).then(data => searchData = data).catch(e => console.error("Search error", e)); searchInput.addEventListener("input", (e) => { const term = e.target.value.toLowerCase(); if (!term) { searchResults.innerHTML = ""; return; } const filtered = searchData.filter(i => i.title.toLowerCase().includes(term) || i.keywords.toLowerCase().includes(term)).slice(0, 10); searchResults.innerHTML = filtered.map(i => '<a href="' + ROOT_PATH + i.url + '" class="search-result-item"><span class="search-result-title">' + i.title + '</span><span class="search-result-type">' + i.type + '</span></a>').join(""); }); } });